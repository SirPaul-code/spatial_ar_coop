name: Server container

on:
  push:
    branches: [main]
    paths:
      - 'server/**'
      - 'docker-compose.yml'
      - '.github/workflows/container.yml'
  pull_request:
    paths:
      - 'server/**'
      - 'docker-compose.yml'
      - '.github/workflows/container.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  docker:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build relay/map server image
        uses: docker/build-push-action@v6
        with:
          context: server
          push: false
          tags: spatial-ar-coop-server:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max
