export function opsPage() {
  return `<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Spatial Ops · Hi-Fi 3D COP</title>
<style>
:root{color-scheme:dark;--bg:#070a0d;--panel:#0c1116;--panel2:#111920;--line:#21313b;--text:#e9eef1;--muted:#8295a0;--cyan:#52e3d4;--green:#78e797;--amber:#f3b75b;--red:#ff746d;--blue:#70aaff}
*{box-sizing:border-box}html,body{margin:0;width:100%;height:100%;overflow:hidden;background:var(--bg);color:var(--text);font:12px Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}button,input,select{font:inherit;color:inherit}button,select,input[type=password]{background:var(--panel2);border:1px solid var(--line);border-radius:6px;padding:7px 9px}button{cursor:pointer}button:hover,button.active{border-color:var(--cyan);color:var(--cyan)}.mono{font-family:ui-monospace,SFMono-Regular,Consolas,monospace}.muted{color:var(--muted)}.ok{color:var(--green)}.warn{color:var(--amber)}.bad{color:var(--red)}.hidden{display:none!important}
.app{height:100vh;display:grid;grid-template-rows:50px 1fr 72px}.topbar{display:flex;align-items:center;gap:9px;padding:7px 11px;background:#090d11;border-bottom:1px solid var(--line)}.brand{min-width:250px;display:flex;gap:9px;align-items:center}.mark{width:9px;height:29px;background:linear-gradient(180deg,var(--cyan),#154d54);box-shadow:0 0 16px rgba(82,227,212,.25)}.brandTitle{font-size:15px;font-weight:850;letter-spacing:.09em}.brandSub{font-size:8px;color:var(--muted);letter-spacing:.18em}.grow{flex:1}.chip{padding:5px 8px;border:1px solid var(--line);border-radius:999px;color:var(--muted);white-space:nowrap}.chip.live{color:var(--green);border-color:#27523a}.mapSelect{min-width:210px}
.workspace{min-height:0;display:grid;grid-template-columns:255px minmax(400px,1fr) 320px}.left,.right{min-height:0;overflow:auto;background:var(--panel);padding:9px}.left{border-right:1px solid var(--line)}.right{border-left:1px solid var(--line)}.sceneWrap{position:relative;min-width:0;min-height:0;overflow:hidden;background:radial-gradient(circle at 50% 45%,#111a21,#06090c 72%)}canvas{position:absolute;inset:0;width:100%;height:100%;display:block;touch-action:none;cursor:crosshair}.toolbar{position:absolute;left:10px;top:10px;display:flex;gap:5px;padding:5px;background:rgba(7,10,13,.86);border:1px solid var(--line);border-radius:7px;backdrop-filter:blur(8px)}.hud{position:absolute;left:12px;bottom:11px;padding:7px 9px;background:rgba(7,10,13,.89);border-left:2px solid var(--cyan);color:var(--muted);max-width:78%;font-size:10px;line-height:1.45}.crosshair{position:absolute;left:50%;top:50%;width:18px;height:18px;transform:translate(-50%,-50%);pointer-events:none;opacity:.35}.crosshair:before,.crosshair:after{content:"";position:absolute;background:#dbe4e8}.crosshair:before{left:8px;top:0;width:1px;height:18px}.crosshair:after{left:0;top:8px;width:18px;height:1px}
.section{margin-bottom:10px;border:1px solid var(--line);background:#0a0f13}.sectionTitle{display:flex;justify-content:space-between;align-items:center;padding:7px 8px;border-bottom:1px solid var(--line);font-size:9px;letter-spacing:.13em;text-transform:uppercase;color:#9badb6}.sectionBody{padding:7px}.metricGrid{display:grid;grid-template-columns:1fr 1fr;gap:5px}.metric{padding:7px;background:#0f171c;border:1px solid #1b2931}.metric b{display:block;font-size:18px}.metric span{font-size:8px;text-transform:uppercase;color:var(--muted)}.row{display:flex;gap:6px;align-items:center;padding:5px 0;border-bottom:1px solid #172229}.row:last-child{border:0}.dot{width:7px;height:7px;border-radius:50%;background:var(--muted)}.dot.ok{background:var(--green)}.dot.warn{background:var(--amber)}.name{min-width:0;flex:1;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.tiny{font-size:9px;color:var(--muted)}.layers{display:grid;grid-template-columns:1fr 1fr;gap:5px}.layers label{display:flex;gap:5px;align-items:center;color:var(--muted);font-size:9px}.layers input{accent-color:var(--cyan)}.kv{display:grid;grid-template-columns:1fr auto;gap:5px 8px}.kv div:nth-child(odd){color:var(--muted)}
.entity{padding:7px;margin-bottom:5px;border:1px solid var(--line);background:#0d1419;cursor:pointer}.entity:hover,.entity.selected{border-color:var(--cyan);background:#101b20}.entityHead{display:flex;align-items:center;gap:6px}.entityLabel{font-weight:800;text-transform:uppercase}.quality{margin-left:auto;padding:2px 4px;border:1px solid var(--line);border-radius:3px;font-size:7px}.quality.multi{color:var(--green);border-color:#28513a}.quality.conflict{color:var(--red);border-color:#5a2c2b}.entityMeta{display:flex;gap:8px;margin-top:4px;font-size:8px;color:var(--muted)}.obs{margin-top:6px;padding:5px;border-left:2px solid #294a51;background:#090f13}.sourceTag{font-size:8px;color:var(--cyan)}
.timeline{display:grid;grid-template-columns:245px 1fr 310px;align-items:center;gap:10px;padding:8px 11px;border-top:1px solid var(--line);background:#090d11}.timelineControls{display:flex;gap:5px;align-items:center}.timelineMain input{width:100%;accent-color:var(--cyan)}.timelineLabels{display:flex;justify-content:space-between;color:var(--muted);font-size:8px}.timelineState{text-align:right;color:var(--muted);font-size:9px}.timelineState b{color:var(--text)}
.modalBg{position:fixed;inset:0;z-index:30;display:flex;align-items:center;justify-content:center;background:rgba(0,0,0,.73)}.modal{width:min(430px,calc(100% - 30px));padding:17px;background:var(--panel);border:1px solid var(--line)}.modal h2{margin:0 0 7px}.modal input{width:100%;margin:9px 0}.actions{display:flex;gap:6px}.actions button{flex:1}.error{min-height:15px;color:var(--red);margin-top:6px}
@media(max-width:1000px){.workspace{grid-template-columns:220px 1fr}.right{display:none}.timeline{grid-template-columns:210px 1fr}.timelineState{display:none}.brand{min-width:175px}.brandSub{display:none}}
</style>
</head>
<body>
<div class="app">
<header class="topbar"><div class="brand"><div class="mark"></div><div><div class="brandTitle">SPATIAL OPS · HI-FI</div><div class="brandSub">LIVE 3D COMMON OPERATING PICTURE</div></div></div><select id="mapSelect" class="mapSelect" disabled><option>Owner access required</option></select><span id="serverChip" class="chip">discovering</span><span id="modeChip" class="chip live">LIVE</span><span class="grow"></span><button id="worldDebugger">World debugger</button><button id="ownerAccess">Owner access</button></header>
<main class="workspace">
<aside class="left">
<section class="section"><div class="sectionTitle"><span>Fusion</span><span id="fusionAge" class="tiny">—</span></div><div class="sectionBody"><div class="metricGrid"><div class="metric"><b id="metricSensors">0</b><span>sensors</span></div><div class="metric"><b id="metricLocalized">0</b><span>localized</span></div><div class="metric"><b id="metricRaw">0</b><span>observations</span></div><div class="metric"><b id="metricFused">0</b><span>entities</span></div></div><div class="row"><span class="dot ok"></span><span class="name">Multi-sensor confirmed</span><b id="metricMulti">0</b></div></div></section>
<section class="section"><div class="sectionTitle"><span>Sensor mesh</span><span id="sensorCount" class="tiny">0 online</span></div><div id="sensors" class="sectionBody"></div></section>
<section class="section"><div class="sectionTitle"><span>World layers</span></div><div class="sectionBody"><div class="layers"><label><input type="checkbox" data-layer="points" checked>Point cloud</label><label><input type="checkbox" data-layer="grid" checked>Ground grid</label><label><input type="checkbox" data-layer="anchors" checked>Anchors</label><label><input type="checkbox" data-layer="clients" checked>Sensors</label><label><input type="checkbox" data-layer="links" checked>Provenance</label><label><input type="checkbox" data-layer="tracks" checked>Entities</label><label><input type="checkbox" data-layer="uncertainty" checked>Uncertainty</label><label><input type="checkbox" data-layer="trails" checked>Trails</label><label><input type="checkbox" data-layer="labels" checked>Labels</label><label><input type="checkbox" data-layer="poses" checked>3D poses</label></div></div></section>
<section class="section"><div class="sectionTitle"><span>Navigation</span></div><div class="sectionBody tiny">Free fly: drag to look · WASD move · Q/E down/up · Shift boost · mouse wheel dolly. You can fly directly inside the point cloud.</div></section>
<section class="section"><div class="sectionTitle"><span>Map</span></div><div id="mapFacts" class="sectionBody kv"><div>Anchors</div><b>—</b><div>Scan points</div><b>—</b><div>Replay</div><b>—</b></div></section>
</aside>
<section class="sceneWrap"><canvas id="scene"></canvas><div class="crosshair"></div><div class="toolbar"><button id="viewFly" class="active">Free fly 3D</button><button id="viewTop">Top-down</button><button id="fit">Fit world</button><button id="liveNow">Live</button></div><div id="sceneHud" class="hud">Authenticate to enter the spatial world.</div></section>
<aside class="right"><section class="section"><div class="sectionTitle"><span>Fused entities</span><span id="entityCount" class="tiny">0</span></div><div id="entities" class="sectionBody"></div></section><section class="section"><div class="sectionTitle"><span>Entity inspector</span></div><div id="inspector" class="sectionBody"><span class="muted">Select an entity.</span></div></section></aside>
</main>
<footer class="timeline"><div class="timelineControls"><button id="play">▶</button><button id="jumpLive">LIVE</button><span class="mono" id="replayClock">--:--:--</span></div><div class="timelineMain"><input id="timeline" type="range" min="0" max="0" value="0" step="1" disabled><div class="timelineLabels"><span id="oldest">-10m</span><span id="newest">now</span></div></div><div class="timelineState"><b id="timelineMode">LIVE · INTERPOLATED</b><br><span id="timelineDetail">waiting for snapshots</span></div></footer>
</div>
<div id="authGate" class="modalBg"><div class="modal"><h2>Owner access</h2><div class="muted">Use the server owner token. It stays in this browser only.</div><input id="token" type="password" placeholder="sar_admin_…"><label class="tiny"><input id="remember" type="checkbox"> Remember on this device</label><div id="authError" class="error"></div><div class="actions"><button id="connect">Connect</button><button id="closeAuth">Close</button></div></div></div>
<script>
const $=id=>document.getElementById(id);
const LIVE_POLL_MS=100,INTERPOLATION_DELAY_MS=115,MAX_EXTRAPOLATION_MS=320,UI_INTERVAL_MS=240;
const sessionKey='spatialOwnerToken',storedKey='spatialOwnerTokenRemembered';
const state={server:null,maps:[],map:null,cloud:null,samples:[],history:[],selectedId:null,mode:'live',replayIndex:0,playing:false,view:'fly',keys:new Set(),drag:null,lastUiAt:0,lastFrameAt:performance.now(),fps:0,fpsFrames:0,fpsAt:performance.now(),camera:{pos:[0,2,-8],yaw:0,pitch:-.12,fov:62,speed:6},top:{center:[0,0,0],scale:20},layers:{points:true,grid:true,anchors:true,clients:true,links:true,tracks:true,uncertainty:true,trails:true,labels:true,poses:true}};
const token=()=>sessionStorage.getItem(sessionKey)||localStorage.getItem(storedKey)||'';
function setToken(v,remember){sessionStorage.setItem(sessionKey,v);if(remember)localStorage.setItem(storedKey,v);else localStorage.removeItem(storedKey)}
function headers(){const v=token();return v?{Authorization:'Bearer '+v}:{}}
async function get(path){const r=await fetch(path,{headers:headers(),cache:'no-store'});if(!r.ok){let m='HTTP '+r.status;try{m=(await r.json()).error?.message||m}catch{}throw new Error(m)}return r.json()}
function esc(v){return String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]))}
function short(v,n=16){v=String(v||'');return v.length>n?v.slice(0,6)+'…'+v.slice(-5):v}
function age(ms){if(!Number.isFinite(ms))return '—';if(ms<1000)return Math.max(0,Math.round(ms))+'ms';if(ms<60000)return(ms/1000).toFixed(1)+'s';return Math.floor(ms/60000)+'m'}
function fmt(v,d=2){return Number.isFinite(Number(v))?Number(v).toFixed(d):'—'}
function clamp(v,a,b){return Math.min(b,Math.max(a,v))}
function v3(v){return Array.isArray(v)&&v.length>=3?[+v[0]||0,+v[1]||0,+v[2]||0]:[0,0,0]}
function add(a,b){return[a[0]+b[0],a[1]+b[1],a[2]+b[2]]}function sub(a,b){return[a[0]-b[0],a[1]-b[1],a[2]-b[2]]}function mul(a,s){return[a[0]*s,a[1]*s,a[2]*s]}function dot(a,b){return a[0]*b[0]+a[1]*b[1]+a[2]*b[2]}function len(a){return Math.hypot(a[0],a[1],a[2])}function norm(a){const l=len(a)||1;return mul(a,1/l)}function mix(a,b,t){return a+(b-a)*t}function mix3(a,b,t){a=v3(a);b=v3(b);return[mix(a[0],b[0],t),mix(a[1],b[1],t),mix(a[2],b[2],t)]}
function matrixPos(m){return Array.isArray(m)&&m.length===16?[+m[12]||0,+m[13]||0,+m[14]||0]:[0,0,0]}
function nlerpQuat(a,b,t){if(!Array.isArray(a)||a.length<4)return b||[0,0,0,1];if(!Array.isArray(b)||b.length<4)return a;let sign=(a[0]*b[0]+a[1]*b[1]+a[2]*b[2]+a[3]*b[3])<0?-1:1;let q=[mix(+a[0]||0,(+b[0]||0)*sign,t),mix(+a[1]||0,(+b[1]||0)*sign,t),mix(+a[2]||0,(+b[2]||0)*sign,t),mix(+a[3]||1,(+b[3]||1)*sign,t)];const l=Math.hypot(q[0],q[1],q[2],q[3])||1;return q.map(x=>x/l)}
function rotateQuat(v,q){if(!Array.isArray(q)||q.length<4)return v;const x=+q[0]||0,y=+q[1]||0,z=+q[2]||0,w=+q[3]||1,tx=2*(y*v[2]-z*v[1]),ty=2*(z*v[0]-x*v[2]),tz=2*(x*v[1]-y*v[0]);return[v[0]+w*tx+(y*tz-z*ty),v[1]+w*ty+(z*tx-x*tz),v[2]+w*tz+(x*ty-y*tx)]}
function mixAngle(a,b,t){let d=(b-a+Math.PI)%(Math.PI*2)-Math.PI;if(d<-Math.PI)d+=Math.PI*2;return a+d*t}
function mixPose(a,b,t){const ma=new Map((a||[]).map(j=>[j[0],j])),mb=new Map((b||[]).map(j=>[j[0],j])),out=[];for(const [i,jb] of mb){const ja=ma.get(i);if(!ja){out.push(jb.slice());continue}out.push([i,mix(ja[1],jb[1],t),mix(ja[2],jb[2],t),mix(ja[3],jb[3],t),mix(ja[4],jb[4],t)])}return out}
function mixFrame(a,b,t){if(!a)return b;if(!b)return a;const ac=new Map((a.clients||[]).map(c=>[c.clientId,c]));const clients=(b.clients||[]).map(c=>{const p=ac.get(c.clientId);if(!p?.pose||!c.pose)return c;return{...c,pose:{...c.pose,position:mix3(p.pose.position,c.pose.position,t),rotation:nlerpQuat(p.pose.rotation,c.pose.rotation,t)}}});const at=new Map((a.fusedTracks||[]).map(x=>[x.id,x]));const fusedTracks=(b.fusedTracks||[]).map(x=>{const p=at.get(x.id);if(!p)return x;return{...x,position:mix3(p.position,x.position,t),velocity:mix3(p.velocity,x.velocity,t),extentMeters:mix3(p.extentMeters,x.extentMeters,t),confidence:mix(p.confidence||0,x.confidence||0,t),uncertaintyMeters:mix(p.uncertaintyMeters||0,x.uncertaintyMeters||0,t),yawRadians:mixAngle(p.yawRadians||0,x.yawRadians||0,t),poseJoints:mixPose(p.poseJoints,x.poseJoints,t)}});return{...b,serverTimeMs:mix(a.serverTimeMs||0,b.serverTimeMs||0,t),clients,fusedTracks}}
function extrapolate(frame,ms){if(!frame||ms<=0)return frame;const dt=Math.min(MAX_EXTRAPOLATION_MS,ms)/1000;return{...frame,fusedTracks:(frame.fusedTracks||[]).map(t=>({...t,position:add(v3(t.position),mul(v3(t.velocity),dt))}))}}
function liveFrame(nowPerf){const s=state.samples;if(!s.length)return null;if(s.length===1)return s[0].data;const target=nowPerf-INTERPOLATION_DELAY_MS;let before=s[0],after=null;for(let i=1;i<s.length;i++){if(s[i].at>=target){after=s[i];break}before=s[i]}if(after){const span=Math.max(1,after.at-before.at);return mixFrame(before.data,after.data,clamp((target-before.at)/span,0,1))}const latest=s[s.length-1];return extrapolate(latest.data,target-latest.at)}
function currentFrame(nowPerf=performance.now()){if(state.mode==='live')return liveFrame(nowPerf);return state.history[Math.max(0,Math.min(state.history.length-1,state.replayIndex))]||liveFrame(nowPerf)}
async function discover(){try{state.server=await(await fetch('/api/v1/info',{cache:'no-store'})).json();$('serverChip').textContent=(state.server.serverName||'server')+' · '+short(state.server.serverId,20);if(token()){$('token').value=token();$('remember').checked=Boolean(localStorage.getItem(storedKey));await connectOwner(true)}}catch(e){$('serverChip').textContent=e.message}}
async function connectOwner(silent=false){const v=$('token').value.trim()||token();if(!v){if(!silent)$('authError').textContent='Owner token required.';return}setToken(v,$('remember').checked);try{await refreshMaps();$('authGate').classList.add('hidden')}catch(e){$('authError').textContent=e.message;$('authGate').classList.remove('hidden')}}
async function refreshMaps(){const data=await get('/api/v1/maps');state.maps=data.maps||[];const old=state.map?.id;state.map=state.maps.find(m=>m.id===old)||state.maps[0]||null;const s=$('mapSelect');s.disabled=false;s.innerHTML=state.maps.length?state.maps.map(m=>'<option value="'+esc(m.id)+'">'+esc(m.name)+' · '+esc(m.status)+'</option>').join(''):'<option>No maps</option>';if(state.map){s.value=state.map.id;await loadMap()}}
async function loadMap(){if(!state.map)return;state.samples=[];state.history=[];state.selectedId=null;state.mode='live';try{state.cloud=await get('/api/v1/maps/'+encodeURIComponent(state.map.id)+'/point-cloud?maxPoints=50000')}catch{state.cloud=null}fitWorld();await Promise.all([refreshLive(),refreshReplay()]);updateUi(performance.now())}
let liveBusy=false;async function refreshLive(){if(!state.map||liveBusy)return;liveBusy=true;try{const data=await get('/api/v1/maps/'+encodeURIComponent(state.map.id)+'/ops-state');state.samples.push({at:performance.now(),data});if(state.samples.length>12)state.samples.splice(0,state.samples.length-12)}catch(e){$('sceneHud').textContent='Ops state unavailable: '+e.message}finally{liveBusy=false}}
async function refreshReplay(){if(!state.map)return;try{const r=await get('/api/v1/maps/'+encodeURIComponent(state.map.id)+'/mission-replay?seconds=600&limit=1200');state.history=r.frames||[];const s=$('timeline');s.max=Math.max(0,state.history.length-1);if(state.mode==='live')s.value=s.max;s.disabled=!state.history.length;renderTimeline()}catch(e){$('timelineDetail').textContent='Replay unavailable: '+e.message}}
function worldBounds(){const all=[],pts=state.cloud?.points||[],anchors=state.map?.anchors||[];const stride=Math.max(1,Math.ceil(pts.length/4000));for(let i=0;i<pts.length;i+=stride)all.push(v3(pts[i]));for(const a of anchors)all.push(matrixPos(a.siteFromAnchor));if(!all.length)return{min:[-5,-1,-5],max:[5,3,5],center:[0,1,0],span:10};let mn=[Infinity,Infinity,Infinity],mx=[-Infinity,-Infinity,-Infinity];for(const p of all)for(let i=0;i<3;i++){mn[i]=Math.min(mn[i],p[i]);mx[i]=Math.max(mx[i],p[i])}const center=[(mn[0]+mx[0])/2,(mn[1]+mx[1])/2,(mn[2]+mx[2])/2],span=Math.max(4,mx[0]-mn[0],mx[1]-mn[1],mx[2]-mn[2]);return{min:mn,max:mx,center,span}}
function fitWorld(){const b=worldBounds();state.top.center=b.center.slice();state.top.scale=Math.max(5,b.span);state.camera.pos=[b.center[0],b.center[1]+b.span*.22,b.min[2]-b.span*.72];state.camera.yaw=0;state.camera.pitch=Math.atan2(b.center[1]-state.camera.pos[1],Math.max(.1,b.center[2]-state.camera.pos[2]));state.camera.speed=clamp(b.span*.32,2.5,14)}
function cameraBasis(){const c=Math.cos(state.camera.pitch),s=Math.sin(state.camera.pitch),sy=Math.sin(state.camera.yaw),cy=Math.cos(state.camera.yaw),forward=norm([sy*c,s,cy*c]),right=norm([cy,0,-sy]),up=norm([right[1]*forward[2]-right[2]*forward[1],right[2]*forward[0]-right[0]*forward[2],right[0]*forward[1]-right[1]*forward[0]]);return{forward,right,up}}
function project3(p,w,h){p=v3(p);if(state.view==='top'){const scale=Math.min(w,h)/state.top.scale,x=p[0]-state.top.center[0],z=p[2]-state.top.center[2];return[w/2+x*scale,h/2+z*scale,scale,1]}const basis=cameraBasis(),r=sub(p,state.camera.pos),z=dot(r,basis.forward);if(z<.06)return null;const f=.5*h/Math.tan(state.camera.fov*Math.PI/360);return[w/2+dot(r,basis.right)*f/z,h/2-dot(r,basis.up)*f/z,f/z,z]}
function moveCamera(dt){if(state.view!=='fly'||!state.keys.size)return;const b=cameraBasis(),boost=state.keys.has('ShiftLeft')||state.keys.has('ShiftRight')?3:1,s=state.camera.speed*boost*dt;let d=[0,0,0];if(state.keys.has('KeyW'))d=add(d,b.forward);if(state.keys.has('KeyS'))d=sub(d,b.forward);if(state.keys.has('KeyD'))d=add(d,b.right);if(state.keys.has('KeyA'))d=sub(d,b.right);if(state.keys.has('KeyE'))d[1]+=1;if(state.keys.has('KeyQ'))d[1]-=1;if(len(d)>0)state.camera.pos=add(state.camera.pos,mul(norm(d),s))}
function updateUi(now){const f=currentFrame(now)||{clients:[],fusedTracks:[],fusion:{}};const fusion=f.fusion||{};$('metricSensors').textContent=fusion.connectedClientCount??(f.clients||[]).length;$('metricLocalized').textContent=fusion.localizedClientCount??(f.clients||[]).filter(c=>c.pose).length;$('metricRaw').textContent=fusion.rawObservationCount??f.rawTrackCount??0;$('metricFused').textContent=fusion.fusedEntityCount??(f.fusedTracks||[]).length;$('metricMulti').textContent=fusion.multiSensorEntityCount??(f.fusedTracks||[]).filter(t=>t.sourceCount>1).length;$('fusionAge').textContent=f.serverTimeMs?age(Date.now()-f.serverTimeMs):'—';renderSensors(f);renderEntities(f);renderInspector(f);renderMapFacts(f);renderTimeline()}
function renderSensors(f){const c=f.clients||[];$('sensorCount').textContent=c.length+' online';$('sensors').innerHTML=c.length?c.map(x=>{const tr=String(x.pose?.tracking||'UNLOCALIZED'),cls=tr==='TRACKING'?'ok':x.pose?'warn':'bad';return'<div class="row"><span class="dot '+cls+'"></span><div class="name"><b>'+esc(short(x.clientId,16))+'</b><div class="tiny">'+esc(x.role||'participant')+' · '+esc(tr)+'</div></div><div class="tiny">'+esc(x.status?.state||'connected')+'</div></div>'}).join(''):'<span class="muted">No clients connected.</span>'}
function renderEntities(f){const tracks=f.fusedTracks||[];$('entityCount').textContent=tracks.length;$('entities').innerHTML=tracks.length?tracks.map(t=>{const q=t.quality==='MULTI_SENSOR'?'multi':t.quality==='CONFLICT'?'conflict':'',sel=t.id===state.selectedId?' selected':'',pose=(t.poseJoints||[]).length;return'<div class="entity'+sel+'" data-entity="'+esc(t.id)+'"><div class="entityHead"><span class="entityLabel">'+esc(t.label)+'</span><span class="mono tiny">'+esc(t.id)+'</span><span class="quality '+q+'">'+esc(t.quality||'SINGLE')+'</span></div><div class="entityMeta"><span>'+Math.round((t.confidence||0)*100)+'%</span><span>±'+fmt(t.uncertaintyMeters,2)+'m</span><span>'+Number(t.sourceCount||0)+' src</span>'+(pose?'<span>'+pose+' joints</span>':'')+'</div></div>'}).join(''):'<span class="muted">No fused entities.</span>';document.querySelectorAll('[data-entity]').forEach(el=>el.onclick=()=>{state.selectedId=el.dataset.entity;updateUi(performance.now())})}
function renderInspector(f){const t=(f.fusedTracks||[]).find(x=>x.id===state.selectedId);if(!t){$('inspector').innerHTML='<span class="muted">Select an entity.</span>';return}let h='<div class="kv"><div>ID</div><b class="mono">'+esc(t.id)+'</b><div>Class</div><b>'+esc(t.label)+'</b><div>Confidence</div><b>'+Math.round((t.confidence||0)*100)+'%</b><div>Agreement</div><b>'+Math.round((t.agreement||0)*100)+'%</b><div>Uncertainty</div><b>±'+fmt(t.uncertaintyMeters,2)+'m</b><div>Pose</div><b>'+Number(t.poseJoints?.length||0)+' joints</b><div>Position XYZ</div><b class="mono">'+v3(t.position).map(x=>fmt(x)).join(' ')+'</b><div>Velocity</div><b class="mono">'+v3(t.velocity).map(x=>fmt(x)).join(' ')+'</b></div><div class="tiny" style="margin-top:9px">PROVENANCE</div>';for(const o of t.observations||[])h+='<div class="obs"><span class="sourceTag">'+esc(short(o.sourceId,15))+'</span> · '+Math.round((o.confidence||0)*100)+'% · ±'+fmt(o.uncertaintyMeters,2)+'m<div class="tiny">'+esc(o.spatialMethod||'unknown')+' · '+Number(o.poseJointCount||0)+' pose joints · hits '+Number(o.hitCount||0)+'</div></div>';$('inspector').innerHTML=h}
function renderMapFacts(f){const m=state.map||{},fu=f.fusion||{};$('mapFacts').innerHTML='<div>Anchors</div><b>'+Number(m.anchors?.length||0)+'</b><div>Scan points</div><b>'+Number(m.scan?.pointCount||0).toLocaleString()+'</b><div>Replay</div><b>'+Number(fu.replayFrameCount??state.history.length)+' frames</b>'}
function renderTimeline(){const s=$('timeline'),h=state.history;if(!h.length){$('replayClock').textContent='--:--:--';$('timelineDetail').textContent='No replay frames yet';return}if(state.mode==='live'){state.replayIndex=h.length-1;s.value=state.replayIndex}const f=h[clamp(state.replayIndex,0,h.length-1)],d=new Date(f.serverTimeMs);$('replayClock').textContent=d.toLocaleTimeString();$('oldest').textContent=new Date(h[0].serverTimeMs).toLocaleTimeString();$('newest').textContent=new Date(h[h.length-1].serverTimeMs).toLocaleTimeString();$('timelineMode').textContent=state.mode==='live'?'LIVE · INTERPOLATED':'MISSION REPLAY';$('timelineDetail').textContent=(state.mode==='live'?'10 Hz snapshots · 60 FPS render':'frame '+(state.replayIndex+1)+' / '+h.length);$('modeChip').textContent=state.mode==='live'?'LIVE':'REPLAY';$('modeChip').className='chip '+(state.mode==='live'?'live':'');$('play').textContent=state.playing?'❚❚':'▶'}
function line3(ctx,a,b,w,h,color,width=1){const pa=project3(a,w,h),pb=project3(b,w,h);if(!pa||!pb)return;ctx.strokeStyle=color;ctx.lineWidth=width;ctx.beginPath();ctx.moveTo(pa[0],pa[1]);ctx.lineTo(pb[0],pb[1]);ctx.stroke()}
function drawGrid(ctx,w,h){if(!state.layers.grid)return;const b=worldBounds(),step=Math.max(1,Math.pow(2,Math.floor(Math.log2(b.span/12)))),half=Math.ceil(b.span*.8/step)*step,y=Number(state.map?.groundY??b.min[1]);ctx.save();for(let x=-half;x<=half;x+=step)line3(ctx,[b.center[0]+x,y,b.center[2]-half],[b.center[0]+x,y,b.center[2]+half],w,h,'rgba(55,82,96,.24)');for(let z=-half;z<=half;z+=step)line3(ctx,[b.center[0]-half,y,b.center[2]+z],[b.center[0]+half,y,b.center[2]+z],w,h,'rgba(55,82,96,.24)');ctx.restore()}
function drawPoints(ctx,w,h){if(!state.layers.points)return;const pts=state.cloud?.points||[];if(!pts.length)return;const stride=Math.max(1,Math.ceil(pts.length/18000)),bounds=state.cloud?.bounds,minY=bounds?.min?.[1]??-1,maxY=bounds?.max?.[1]??1,span=Math.max(.1,maxY-minY);ctx.save();for(let i=0;i<pts.length;i+=stride){const p=pts[i],q=Number(p[3]??.3);if(q<.16)continue;const pr=project3(p,w,h);if(!pr||pr[0]<-4||pr[0]>w+4||pr[1]<-4||pr[1]>h+4)continue;const t=clamp((p[1]-minY)/span,0,1),a=.13+.45*Math.min(1,q),r=state.view==='fly'?clamp(pr[2]*.012,.7,2.4):1.2;ctx.fillStyle='hsla('+(165+44*t)+',58%,'+(43+18*t)+'%,'+a+')';ctx.fillRect(pr[0]-r*.5,pr[1]-r*.5,r,r)}ctx.restore()}
function drawAnchors(ctx,w,h){if(!state.layers.anchors)return;ctx.save();ctx.font='9px ui-monospace,monospace';for(const a of state.map?.anchors||[]){const wp=matrixPos(a.siteFromAnchor),p=project3(wp,w,h);if(!p)continue;const color=a.featureQuality==='GOOD'?'#78e797':a.featureQuality==='SUFFICIENT'?'#f3b75b':'#8295a0';ctx.strokeStyle=color;ctx.fillStyle=color;ctx.lineWidth=1.5;ctx.beginPath();ctx.arc(p[0],p[1],5,0,Math.PI*2);ctx.stroke();line3(ctx,wp,add(wp,[0,.45,0]),w,h,color,1.2);if(state.layers.labels)ctx.fillText('A '+short(a.id,10)+' '+String(a.featureQuality||''),p[0]+7,p[1]-6)}ctx.restore()}
function drawClients(ctx,w,h,f){if(!state.layers.clients)return;ctx.save();ctx.font='9px ui-monospace,monospace';for(const c of f.clients||[]){if(!c.pose?.position)continue;const wp=v3(c.pose.position),p=project3(wp,w,h);if(!p)continue;const fw=rotateQuat([0,0,-1],c.pose.rotation),fp=project3(add(wp,mul(fw,.8)),w,h);ctx.fillStyle='#52e3d4';ctx.strokeStyle='#52e3d4';ctx.beginPath();ctx.arc(p[0],p[1],4,0,Math.PI*2);ctx.fill();if(fp){ctx.beginPath();ctx.moveTo(p[0],p[1]);ctx.lineTo(fp[0],fp[1]);ctx.stroke()}if(state.layers.labels)ctx.fillText('S '+short(c.clientId,12),p[0]+6,p[1]-6)}ctx.restore()}
function drawLinks(ctx,w,h,f){if(!state.layers.links)return;const clients=new Map((f.clients||[]).filter(c=>c.pose?.position).map(c=>[c.clientId,c]));ctx.save();ctx.setLineDash([3,5]);for(const t of f.fusedTracks||[])for(const sid of t.sourceIds||[]){const c=clients.get(sid);if(c)line3(ctx,c.pose.position,t.position,w,h,t.sourceCount>1?'rgba(120,231,151,.34)':'rgba(82,227,212,.17)',1)}ctx.restore()}
const SKELETON_EDGES=[[11,12],[11,13],[13,15],[12,14],[14,16],[11,23],[12,24],[23,24],[23,25],[25,27],[27,31],[24,26],[26,28],[28,32],[0,11],[0,12]];
function drawPose(ctx,t,w,h,color){const joints=t.poseJoints||[];if(joints.length<6)return false;const root=v3(t.position),m=new Map();for(const j of joints){if(Number(j[4]||0)<.20)continue;const p=project3(add(root,[+j[1]||0,+j[2]||0,+j[3]||0]),w,h);if(p)m.set(j[0],p)}if(m.size<6)return false;ctx.strokeStyle=color;ctx.fillStyle=color;ctx.lineWidth=2;for(const e of SKELETON_EDGES){const a=m.get(e[0]),b=m.get(e[1]);if(!a||!b)continue;ctx.beginPath();ctx.moveTo(a[0],a[1]);ctx.lineTo(b[0],b[1]);ctx.stroke()}for(const p of m.values()){ctx.beginPath();ctx.arc(p[0],p[1],2.4,0,Math.PI*2);ctx.fill()}const head=m.get(0);if(head){ctx.beginPath();ctx.arc(head[0],head[1],6,0,Math.PI*2);ctx.stroke()}return true}
function cuboidCorners(t){const p=v3(t.position),e=v3(t.extentMeters||[.7,.7,.7]),hw=e[0]/2,hd=e[2]/2,yaw=Number(t.yawRadians||0),c=Math.cos(yaw),s=Math.sin(yaw),out=[];for(const y of [0,e[1]])for(const z of [-hd,hd])for(const x of [-hw,hw]){const rx=x*c-z*s,rz=x*s+z*c;out.push([p[0]+rx,p[1]+y,p[2]+rz])}return out}
const CUBE_EDGES=[[0,1],[0,2],[1,3],[2,3],[4,5],[4,6],[5,7],[6,7],[0,4],[1,5],[2,6],[3,7]];
function drawCuboid(ctx,t,w,h,color){const c=cuboidCorners(t),p=c.map(x=>project3(x,w,h));ctx.strokeStyle=color;ctx.lineWidth=1.5;for(const e of CUBE_EDGES){const a=p[e[0]],b=p[e[1]];if(!a||!b)continue;ctx.beginPath();ctx.moveTo(a[0],a[1]);ctx.lineTo(b[0],b[1]);ctx.stroke()}}
function drawTracks(ctx,w,h,f){if(!state.layers.tracks)return;ctx.save();ctx.font='10px ui-monospace,monospace';for(const t of f.fusedTracks||[]){const p=project3(t.position,w,h);if(!p)continue;const selected=t.id===state.selectedId,multi=t.quality==='MULTI_SENSOR',conflict=t.quality==='CONFLICT',color=conflict?'#ff746d':multi?'#78e797':'#f3b75b';if(state.layers.uncertainty){const edge=project3(add(v3(t.position),[Number(t.uncertaintyMeters||.5),0,0]),w,h);if(edge){const r=clamp(Math.abs(edge[0]-p[0]),3,80);ctx.strokeStyle=color+'55';ctx.lineWidth=1;ctx.beginPath();ctx.arc(p[0],p[1],r,0,Math.PI*2);ctx.stroke()}}ctx.lineWidth=selected?3:1.6;let pose=false;if(t.label==='person'&&state.layers.poses)pose=drawPose(ctx,t,w,h,color);if(!pose)drawCuboid(ctx,t,w,h,color);if(state.layers.labels){ctx.fillStyle=color;ctx.fillText(String(t.label).toUpperCase()+' '+short(t.id,10)+' '+Math.round((t.confidence||0)*100)+'% · '+Number(t.sourceCount||0)+'S'+(pose?' · POSE':''),p[0]+7,p[1]-8)}}ctx.restore()}
function drawTrails(ctx,w,h){if(!state.layers.trails||state.history.length<2)return;const by=new Map(),stride=Math.max(1,Math.floor(state.history.length/180));for(let i=0;i<state.history.length;i+=stride)for(const t of state.history[i].fusedTracks||[]){let a=by.get(t.id);if(!a)by.set(t.id,a=[]);a.push(t.position)}ctx.save();ctx.strokeStyle='rgba(112,170,255,.28)';ctx.lineWidth=1.2;for(const pts of by.values()){let started=false;ctx.beginPath();for(const wp of pts){const p=project3(wp,w,h);if(!p){started=false;continue}if(!started){ctx.moveTo(p[0],p[1]);started=true}else ctx.lineTo(p[0],p[1])}ctx.stroke()}ctx.restore()}
function draw(now){const canvas=$('scene'),r=canvas.getBoundingClientRect(),dpr=Math.max(1,Math.min(2,devicePixelRatio||1)),cw=Math.round(r.width*dpr),ch=Math.round(r.height*dpr);if(canvas.width!==cw||canvas.height!==ch){canvas.width=cw;canvas.height=ch}const ctx=canvas.getContext('2d');ctx.setTransform(dpr,0,0,dpr,0,0);const w=r.width,h=r.height;ctx.clearRect(0,0,w,h);const f=currentFrame(now)||{};drawGrid(ctx,w,h);drawPoints(ctx,w,h);drawTrails(ctx,w,h);drawAnchors(ctx,w,h);drawLinks(ctx,w,h,f);drawClients(ctx,w,h,f);drawTracks(ctx,w,h,f);const fu=f.fusion||{},cam=state.camera.pos;$('sceneHud').textContent=(state.mode==='live'?'LIVE':'REPLAY')+' · '+state.fps+' FPS · '+Number(fu.connectedClientCount??f.clients?.length??0)+' sensors · '+Number(fu.rawObservationCount??f.rawTrackCount??0)+' observations → '+Number(fu.fusedEntityCount??f.fusedTracks?.length??0)+' entities · camera '+cam.map(x=>fmt(x,1)).join(' / ')+' · '+(state.view==='fly'?'WASD/QE + drag + wheel':'top-down wheel zoom')}
function tick(now){const dt=Math.min(.05,Math.max(0,(now-state.lastFrameAt)/1000));state.lastFrameAt=now;moveCamera(dt);state.fpsFrames++;if(now-state.fpsAt>=500){state.fps=Math.round(state.fpsFrames*1000/(now-state.fpsAt));state.fpsFrames=0;state.fpsAt=now}draw(now);if(now-state.lastUiAt>UI_INTERVAL_MS){state.lastUiAt=now;updateUi(now)}requestAnimationFrame(tick)}
$('ownerAccess').onclick=()=>{$('authGate').classList.remove('hidden');$('token').focus()};$('closeAuth').onclick=()=>$('authGate').classList.add('hidden');$('connect').onclick=()=>connectOwner(false);$('worldDebugger').onclick=()=>location.href='/';$('mapSelect').onchange=async e=>{state.map=state.maps.find(m=>m.id===e.target.value)||null;await loadMap()};
$('viewFly').onclick=()=>{state.view='fly';$('viewFly').classList.add('active');$('viewTop').classList.remove('active')};$('viewTop').onclick=()=>{state.view='top';$('viewTop').classList.add('active');$('viewFly').classList.remove('active')};$('fit').onclick=fitWorld;$('liveNow').onclick=jumpLive;$('jumpLive').onclick=jumpLive;function jumpLive(){state.mode='live';state.playing=false;updateUi(performance.now())}
$('timeline').oninput=e=>{state.mode='replay';state.playing=false;state.replayIndex=Number(e.target.value)||0;updateUi(performance.now())};$('play').onclick=()=>{if(!state.history.length)return;state.mode='replay';state.playing=!state.playing;renderTimeline()};
document.querySelectorAll('[data-layer]').forEach(el=>el.onchange=()=>{state.layers[el.dataset.layer]=el.checked});
const canvas=$('scene');canvas.onpointerdown=e=>{state.drag={x:e.clientX,y:e.clientY,yaw:state.camera.yaw,pitch:state.camera.pitch,top:state.top.center.slice()};canvas.setPointerCapture(e.pointerId)};canvas.onpointermove=e=>{if(!state.drag)return;const dx=e.clientX-state.drag.x,dy=e.clientY-state.drag.y;if(state.view==='fly'){state.camera.yaw=state.drag.yaw+dx*.0055;state.camera.pitch=clamp(state.drag.pitch-dy*.0045,-1.48,1.48)}else{const rect=canvas.getBoundingClientRect(),scale=Math.min(rect.width,rect.height)/state.top.scale;state.top.center=[state.drag.top[0]-dx/scale,state.drag.top[1],state.drag.top[2]-dy/scale]}};canvas.onpointerup=canvas.onpointercancel=()=>state.drag=null;canvas.onwheel=e=>{e.preventDefault();if(state.view==='fly'){state.camera.pos=add(state.camera.pos,mul(cameraBasis().forward,-e.deltaY*.008*state.camera.speed))}else state.top.scale=clamp(state.top.scale*Math.exp(e.deltaY*.001),1,400)};
window.addEventListener('keydown',e=>{if(['KeyW','KeyA','KeyS','KeyD','KeyQ','KeyE','ShiftLeft','ShiftRight'].includes(e.code)){state.keys.add(e.code);e.preventDefault()}});window.addEventListener('keyup',e=>state.keys.delete(e.code));window.addEventListener('blur',()=>state.keys.clear());
setInterval(()=>{if(state.playing&&state.history.length){state.replayIndex=Math.min(state.history.length-1,state.replayIndex+1);if(state.replayIndex>=state.history.length-1)state.playing=false;$('timeline').value=state.replayIndex;updateUi(performance.now())}},500);setInterval(()=>{if(state.map)refreshLive()},LIVE_POLL_MS);setInterval(()=>{if(state.map)refreshReplay()},4000);discover();requestAnimationFrame(tick);
</script>
</body></html>`;
}
